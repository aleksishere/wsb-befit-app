<h1>wsb-befit-app</h1>
Aplikacja utworzona w .NET 10, wymagany dotnet ef do utworzenia bazy danych.
Uruchom te komendy, aby aplikacja działała poprawnie!

```
dotnet tool install --global dotnet-ef
dotnet ef migrations add MIGRATION_NAME
dotnet ef database update
```
